# md-node

Single process entry point with Kafka-style roles:

```bash
md-node --roles=gateway
md-node --roles=controller
md-node --roles=gateway,controller
```

The current implementation is a placeholder skeleton.
