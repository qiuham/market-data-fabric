# md-service

Gateway runtime and process-level orchestration.

Owns config loading, pipeline building, adapter lifecycle, health, metrics, recorder wiring, and publisher wiring. It must not contain provider-specific field mapping.
