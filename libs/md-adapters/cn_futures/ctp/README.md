# CTP Adapter

Skeleton for China futures CTP market data SDK integration.

Keep CTP SDK headers and binaries out of git unless explicitly approved. This adapter will likely be Linux x86_64 or Windows specific, while the core libraries remain portable.
