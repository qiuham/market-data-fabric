# md-core

HFT-reusable core market data types and interfaces.

Owns standard event structs, IDs, timestamps, flags, sequence metadata, and sink interfaces. Must stay vendor-agnostic, transport-agnostic, allocation-light, and portable across macOS and Linux where possible.
