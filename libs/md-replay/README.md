# md-replay

Recording and replay support for raw provider payloads and normalized events.

Replay should let strategy code consume historical data through the same interface used for live data where practical.
